package com.niyati.solidprinciples.isp;

/**
 * This principle is similar to SRP.
 * If only 1 interface is used, and it consists all methods, and we implement class from that
 * ,then the methods which are not needed will also override.
 * So ,to avoid this issue individual interfaces classes will be created.
 */